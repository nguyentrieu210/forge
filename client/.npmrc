# MetaForge — pnpm config
# strict peer deps off during scaffold; tighten before release
strict-peer-dependencies=false
auto-install-peers=true
# hoist only what UI libs need at root for tooling
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
