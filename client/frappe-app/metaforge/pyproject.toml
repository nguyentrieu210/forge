[project]
name = "metaforge"
authors = [{ name = "MetaForge", email = "brvt67337@gmail.com" }]
description = "Meta-driven React engine copy 1:1 Frappe/ERPNext Desk (headless) — orchestration app"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "metaforge"
