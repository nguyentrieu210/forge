{
  "// why": [
    "Playwright resolves the nearest tsconfig by walking up from each test file. Without",
    "one here it reaches client/tsconfig.json, whose project references it fails to",
    "resolve ('Failed to resolve references path packages/core') even though those",
    "tsconfigs exist and `tsc -b` builds them. A local, reference-free config stops the",
    "walk. This file compiles nothing — Playwright transpiles the specs itself."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "esModuleInterop": true
  },
  "include": ["tests/**/*.ts", "playwright.config.ts"]
}
