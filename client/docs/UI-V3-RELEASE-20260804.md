# Forge UI V3 release marker

Date: 2026-08-04

Purpose: trigger the canonical UI-only production release lane after UI V3 convergence and the root pnpm lockfile were both merged into `main`.

Release contents already merged before this marker:
- V3-01 Foundation
- V3-02 Shell
- V3-03 Auth/Login
- V3-04 Data Surfaces
- V3-05 Charts / Command Center
- V3-06 Builder
- V3-07 Mobile / QA harness
- V3-08 convergence
- root dependency lock alignment

This marker changes no runtime, backend, schema, migration, permission, tenant, or business-rule behavior.
