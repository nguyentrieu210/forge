# MetaForge — Phase Tracker (app-factory)

> Dự án: **MetaForge** — bộ FE React meta-driven copy 1:1 hành vi metadata-driven của Frappe/ERPNext Desk, chạy trên backend **Frappe v16 headless** (VPS 222.255.238.178). Đầu ra: **engine kit tái dùng + 1 app demo**. Luồng: **A (app mới)**.
>
> ⚠️ Adaptation: backend = Frappe thật ⇒ luật backend Cloudflare/D1/Hono của app-factory KHÔNG áp dụng. Giữ quy trình 7 pha + cổng chặn + FE kit React/shadcn; tầng dữ liệu = `frappe-react-sdk` (`/api/resource`, `/api/method`, meta API).

| Pha | Cổng | Trạng thái | Bằng chứng |
|---|---|---|---|
| 0 — Chốt hướng + phạm vi | Scope gate | ✅ DUYỆT | headless Frappe + SPA React; full 1:1 không cắt; engine kit + demo; demo dùng DocType ERPNext có sẵn; VPS 222 (đã đo: 4 vCPU / 5.8GB RAM / 47GB disk free / no swap) |
| 1 — Intake & Research | Cổng 1 | ✅ DUYỆT ("duyệt toàn bộ") | 12 nguồn/5 lớp, bảng xếp hạng 9 hệ-con, chuẩn 1:1 = Desk renderer |
| 2a — Đọc contract (§0) | — | ✅ | Đọc guide + 13 contract, bảng §0 trong BRD.md |
| 2b — BRD spine | — | ✅ | `docs/BRD.md` (11 mục + §0 + §2 + 8 sửa v16) |
| 2c — Screen Spec Cards | — | ✅ 23/23 | `docs/brd-screens/00–22` + `brd-flows/00-core-flows.md` + `brd-entities/00-entities.md` |
| 2d — Scorecard | Cổng 2 | ✅ **DUYỆT** (user standing approval 2026-07-23: "làm nốt xong tao tự duyệt, chạy tiếp không hỏi") | Scorecard §7 ✅; vá review xong; AC 23/23; grep sạch |
| 2i — Vá review độc lập | Cổng 2 | ✅ vá xong, grep sạch | grep-verify 12 fact v16 (16.28.0); B1–B6 + M1–M9 + C3–C6; +appendix §Q/§R/§S + Builder BRD; **grep cuối sạch**; **AC 23/23** |
| 3 — Thiết kế kỹ thuật | Cổng 3 | ✅ **ĐIỀU KIỆN ĐẠT — vá review#4 (7 mục) xong, verified 16.29.0, grep sạch** | **7 mục review#4 (grep-verify live 16.29.0):** 1 Import raw status `Pending/Success/Partial Success/Error/Timed Out` + tách UiPhase · 2 Perm DTO `{roles:{label,value}[],doctypes:{label,value}[],doctype_ptype_map}` (SysMgr) · 3 `getdoctype(doctype,with_parent)` bỏ `cached_timestamp` · 4 `add_node`→`null` (refetch/orch) · 5 session=logout-others-only + backup=`{db,public,private,config}` LIST · 6 api-map↔adapter **1:1** (+§12 notif/workspace/kanban, +§13 amend/builder) · 7 Kanban=`update_order_for_single_card` (orch riêng, KHÔNG workflow). **P1 sạch:** 40→43 fieldtype toàn bộ · bỏ pin "chốt PHA 3" · offline write-disabled V1 · bỏ `...` mơ hồ. |
| 4 — Scaffold | Cổng 4 | ✅ **SCAFFOLD + VERIFY XANH** | pnpm workspace (node24/pnpm9.15 qua corepack): 6 packages (`@metaforge/core,adapter-frappe,controls,views,builder,shell`) + `apps/demo` + `frappe-app/metaforge`. `core` = 43 fieldtype **verified live docfield.json** + meta/doc/error types (mapError §0). `adapter-frappe` = **FrappeAdapter interface đầy đủ** (review#4-correct: raw import status, doctype_ptype_map, add_node void, backup 4-key, kanban moveCard, notif/workspace). `frappe-app` = 5 orch §11 (get_boot/workflow+comment/kanban+comment/add_tree_node/logout_other). **`pnpm verify` (tsc -b) = exit 0 từ clean; `vite build` demo = 38 modules, 144KB, exit 0.** |
| 5 — Build renderer | — | 🟡 **RENDERER CODE XONG — nhưng UI ban đầu = dev-harness (review độc lập đúng); ĐÃ MỞ LẠI → sprint "Product Shell + Detail Layout" (xem PHA 5.5)** | **①Adapter** `FrappeAdapterImpl` (frappe-js-sdk, token-auth headless) full interface + 6 runtime-fix verified 16.29.0 (print `get_html_and_style`{html,style} · `get_workspaces`.pages · boot `get_fullname`+`workspaces.pages` · `logout` `force=True` · `download_template` export_* · `get_desktop_page` {name,title,public}). **②MetaResolver** (`core/meta`): field states hidden/masked/locked/editable + depends_on/mandatory_depends_on/read_only_depends_on + permlevel + docstatus. **③Controls** 27/43 fieldtype thật (16 còn lại = layout/media/child-grid) + registry + fallback + masked/readonly. **④ListView** (cột từ in_list_view, format cell). **⑤FormView** (resolve theo VALUES → depends_on phản ứng; Tab/Section/Column layout; required/error; 417 conflict banner không ghi đè; save changed+modified). **HOTFIX P0 (reviewer, đã vá):** ⓐ permission bỏ fallback `write.size===0` (role read:1/write:0 giờ đúng read-only, không editable) · ⓑ depends_on array-truthiness (mảng rỗng=false) + `fn:` partial + log lỗi eval (không im lặng) · ⓒ layout bỏ tab/section rỗng dẫn đầu + tôn trọng depends_on của Tab/Section Break · ⓓ form reset khi đổi doc + dirty-về-gốc hết dirty + submit validate required + `docname`=doc.name + Dynamic Link target từ field.options + Link sync khi value reset + Datetime convert Frappe↔datetime-local. **BƯỚC 6 — media + child grid:** media controls thật (Attach/Attach Image upload · Image · Signature canvas pad · Barcode · Geolocation navigator; map/scanner = lazy PHA 6) + Icon · **ChildGrid (M12)** data-driven từ child meta (cột in_list_view, cell control inline, thêm/xoá dòng) · **Table + Table MultiSelect** đăng ký ở views (`registerTableControls`/`createFullRegistry`, tránh cycle) · FieldServices thêm `uploadFile`/`getMeta`. **Full registry phủ 36/43 = 100% field-value** (7 còn lại đều layout/Button). **Test: `selfcheck` 31/31.** **BƯỚC 7 — thêm view:** **KanbanView** (M06, cột theo field Select/workflow_state, nhóm card, onMove→`update_order_for_single_card`, chip-lý-do do container áp) · **TreeView** (M09, NestedSet lazy expand/collapse, presentational parent quản expanded) · **ReportView** (M15, columns+result dict/array từ `query_report.run`) · **PrintView** (M13, iframe cô lập srcDoc từ `get_html_and_style`). **Test: `selfcheck` 37/37.** **BƯỚC 8 — DashboardView** (M10, number card + chart bar SVG; Recharts polish sau) + **CalendarView** (M07, lưới tháng, event theo dateField, điều hướng). **Test: `selfcheck` 39/39.** **BƯỚC 9-10 — CODE PHA 5 XONG:** **9/9 view** (+Gantt SVG) · **BuilderKernel** (History undo/redo pure) + **4 builder kéo-thả**: M17 DocType (palette 43 fieldtype→canvas HTML5-DnD→meta, **live FormView preview**), M18 Workflow (states+transitions), M21 Print Format (blocks+order), M22 Dashboard (cards+charts) · **Shell** (AppShell sidebar/topbar, CommandPalette Ctrl+K, theme 3-mode) · **Demo App** = shell hoàn chỉnh nav 13 màn. **Test: `selfcheck` 45/45 xanh** (+6: History, meta-build, DocTypeBuilder/Gantt/AppShell/CommandPalette render). from-scratch `tsc -b` exit 0 · vite **67 modules / 195KB** exit 0. **BƯỚC 11 — POLISH STACK LIBS (thay bản native, giữ chức năng + test):** ✅ TanStack Table→ListView (sort+pagination) · ✅ Recharts→DashboardView (bar/line responsive) · ✅ dnd-kit→DocTypeBuilder (DndContext+Sortable) · ✅ React Flow→WorkflowBuilder (node-graph) · ✅ react-grid-layout→DashboardBuilder (kéo/resize tile) · ✅ **RHF+Zod→FormView** (state+dirty+validate schema) · frappe-gantt = GIỮ SVG native có chủ đích (imperative, React-fit kém). ✅ **Lazy code-split**: builder chunk 271KB tách khỏi main 698KB. **Test: `selfcheck` 46/46 xanh · from-scratch `tsc -b` exit 0 · demo build 931 modules split.** **CÒN LẠI = tích hợp+deploy (cần site cô lập):** cắm FrappeAdapterImpl vào container → smoke live · PHA 6 verify (4 luật trọng yếu + phân quyền) → PHA 7 deploy VPS 222. |
| 5.5 — Product Shell + Detail Layout sprint | — | 🟢 **A+B+C XONG (verified ẢNH + E2E, KHÔNG bằng test-count); builders defer** | **Nguyên nhân mở lại:** review độc lập chỉ ra bản đầu là *dev-harness* (không design-system, inline-style "HTML 2005", LiveApp thiếu shell, Awesomebar mock, AI=0, 3-cột không tồn tại). **A — Foundation:** `@metaforge/ui` (22 shadcn primitive + Tailwind v4 tokens `@source` quét kit, CSS 30KB thật) · Product AppShell (sidebar nhóm/collapse + **drawer mobile**, topbar breadcrumb/Awesomebar/AI/bell/theme/avatar) · Awesomebar cmdk (Ctrl+K & `/`, debounce+AbortController+chống-stale) · router + **DemoShell dùng chung mock & LiveApp** · AI shell placeholder. **B — List+Split:** **List data-table** server-side thật (search/standard-filter/column-picker/checkbox/STT/status-badge/số-phải/ngày/bulk/Σ summary/pagination "X–Y/Z" + **URL state** q/filter/sort/page/selected + localStorage cột) · **SplitView 3 cột** resizable+lưu-layout (≥1280) / list+detail+context-Sheet (768) / stack+drawer (390), Esc order, **click-row mở split KHÔNG chuyển màn** · FormView re-skin (header+tabs sticky shadcn). **C — Context+Workflow:** adapter +5 method (`getTransitions`/`assignRemove`/`addTag`/`removeTag`/`globalSearch`) · **FormActionBar+WorkflowActionBar** metadata/server-driven (docstatus+is_submittable+perms+workflow; `resolveFormActions` có selfcheck) · **ContextPanel** Timeline/comment/assign/attach/tags + **ContextContainer** (docinfo→timeline + comment mutation thật) · **AI tab** placeholder trung thực · re-skin **controls/media + 7 view** (Input/Select/Date/Combobox/Table/Kanban/Tree/Report/Calendar/Dashboard). **Bằng chứng (không test-count):** 14 screenshot đã-xem (list/filter/bulk/split-3cột/mobile/dark/kanban/form-fields/ai-tab) + **Playwright E2E 8/8 xanh** (list.spec + split.spec: filter/URL-reload/search/bulk/split/actions/AI). **Số:** `tsc -b` exit 0 · selfcheck **45/45** (List/Form/Workflow giờ test **LOGIC**) · lint no-native-UI (regex đã vá bắt multi-line): **120→80, TẤT CẢ 80 còn lại nằm trong 4 builder + BuilderRoutes** — product (list/form/detail/controls/views) = **SẠCH**. **+ Re-skin 4 builder (Workflow/DocType/Dashboard/Print) + BuilderRoutes → lint no-native-UI = 0 (gate CỨNG, script exit 1 nếu >0). 120→0 sạch tuyệt đối** (verified ảnh builder-doctype/workflow). **CÒN LẠI (Pha 2):** assign/attach/tag *picker UI* (adapter đã có) · share/connections chưa nối · Live screenshot (cần backend) · AI endpoint thật/notifications/i18n/a11y/virtualize/6 màn thiếu. |
| 6 — Verify + QA | Cổng 6 | 🔵 **SMOKE LIVE PASS x2** (curl + TS adapter thật) | Site cô lập `metaforge.localhost` (VPS 222, frappe 16.28.0 + app metaforge). **(A) curl smoke:** get_logged_user✅ getdoctype ToDo(18 field/2 perm/`__js`)✅ list✅ search_link(LinkResult)✅ orch get_boot✅ **417 TimestampMismatch**✅. **(B) `FrappeAdapterImpl` TS thật** (qua SSH tunnel→backend, header `X-Frappe-Site-Name`+token): getBoot(roles27/ws4)✅ getWorkspaces(4)✅ getMeta✅ getList✅ — `smoke.ts` OK. Adapter thêm `headers` option (site cô lập sau nginx hardcode). Demo có **live-mode** (`VITE_LIVE=1` → LiveApp: MetaForgeProvider+ListContainer+FormContainer, vite proxy tiêm site header+token). `frontend` không hại; tunnel dọn sạch. **(C) PHÂN QUYỀN SERVER verified live** (§F5, ranh giới cuối): lowpriv → `get_roles_and_doctypes` **403** · list `Role` **403** · **field-level masking**: cùng ToDo, Admin thấy `mf_secret='TOP SECRET'`, lowpriv (read permlevel 0) thấy `description` nhưng `mf_secret=None` — server che GIÁ TRỊ permlevel cao (khớp resolver `masked`). **Còn (cần browser):** 4 luật trọng yếu qua UI (3-cột/Kanban-chip/AI/Lịch-sử) + screenshot baseline. |
| 7 — Docs + Release | — | 🟢 **DOCS/RELEASE XONG** | app `metaforge` deploy live · **`docs/RELEASE.md`** (release notes + verify) · **`docs/DEPLOYMENT.md`** (dựng site cô lập + run live + serve) · **`CHANGELOG.md`** · README cập nhật. Baseline v0.1.0: tsc -b exit 0 · selfcheck 46/46 · build 1012 modules code-split · ~6.7K LOC. **Còn (cần browser/maintenance):** serve SPA publicly (nginx route metaforge.localhost) + UI screenshot baseline. |

## Checklist luồng A (app mới) — chép từ flow-checklists (rút gọn)
- [x] Cổng 0 scope
- [x] Cổng 1 research
- [x] Cổng 2 BRD 360° (DUYỆT — user standing approval)
- [x] Cổng 3 thiết kế kỹ thuật (vá review#4 7 mục xong, verified 16.29.0, grep sạch — điều kiện reviewer ĐẠT)
- [x] Cổng 4 scaffold + verify chạy (tsc -b exit 0 + vite build 144KB exit 0)
- [ ] Cổng 6 verify xanh + QA
- [ ] PHA 7 docs + release + publish
